#include <stdio.h>
#include <math.h>

int somma(int a, int b)
{
    return(a+b);
}
int differenza (int a, int b)
{
    return (a-b);
}
int moltiplicazione (int a, int b)
{
    return (a*b)
}
float divisione (int a, int b)
{
    return (a/b)
}
int resto (int a, int b)
{
    return (a%b)
}
int radice (int a, int b)
{
    return (sqrt(a), sqrt(b))
}
int potenza (int a, int b)
{
    return (a*a , b*b)
}

int main()
{
    int a;
    int b;
    printf("inserire due numeri");
    scanf("%d %d", &a &b);
    printf("la somma è return(somma)");
    printf("la differenza è");
    printf("la moltiplicazione è");
    printf("la divisione");
    printf("il resto della divisione è");
    printf("i quadrati dei due numeri sono");
    printf("le radici dei due numeri sono");

}