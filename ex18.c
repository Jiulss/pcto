#include <stdio.h>

int main()
{
    char str[30];
    char br[5];
    char name[20];

printf("vuoi fare un gioco?\n");
scanf("%s", br);
printf("allora dimmi il tuo nome\n");
scanf("%s", name);
printf("Piacere %s, io sono Whitney Houston. Cominciamo.\n", name);
    printf("How will I know?\n");
    scanf("%s", str);
    if(str[]=)
    {
        printf("I save a prayer with every heartbeat\n");
    }
    else
    {
        printf("Davvero non conosci questa canzone.RIACCHIAPPATI!");
    }
}